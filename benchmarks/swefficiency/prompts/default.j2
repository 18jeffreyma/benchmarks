I have access to a python code repository in the directory {{ instance.repo_path }}. You can explore and modify files using the available tools.

Consider the following performance workload and `workload()` function showing a specific usage of the repository:

<performance_workload>
{{ instance.workload }}
</performance_workload>

Can you help me implement the necessary changes to the repository so that the runtime of the `workload()` function is faster? Basic guidelines:

1. Your task is to make changes to non-test files in the {{ instance.repo_path }} directory to improve the performance of the code running in `workload()`. Please do not directly change the implementation of the `workload()` function to optimize things: I want you to focus on making the workload AS IS run faster by only editing the repository containing code that the `workload()` function calls.

2. Make changes while ensuring the repository is functionally equivalent to the original: your changes should not introduce new bugs or cause already-passing tests to begin failing after your changes. However, you do not need to worry about tests that already fail without any changes made. For relevant test files you find in the repository, you can run them via the bash command `{{ instance.test_cmd }} <test_file>` to check for correctness. Note that running all the tests may take a long time, so you need to determine which tests are relevant to your changes.

3. Make sure the `workload()` function improves in performance after you make changes to the repository. The workload can potentially take some time to run, so please allow it to finish and be generous with setting your timeout parameter (a timeout value of 3600 or larger here is encouraged): for faster iteration, you should adjust the workload script to use fewer iterations. Before you complete your task, please make sure to check that the **original performance workload** and `workload()` function runs successfully and the performance is improved.

4. You may need to reinstall/rebuild the repo for your changes to take effect before testing if you made non-Python changes. Reinstalling may take a long time to run (a timeout value of 3600 or larger here is encouraged), so please be patient with running it and allow it to complete if possible. You can reinstall the repository by running the bash command `{{ instance.rebuild_cmd }}` in the workspace directory.

5. All the dependencies required to run the `workload()` function are already installed in the environment. You should not install or upgrade any dependencies.

Follow these phases to improve performance:

Phase 1. EXPLORATION: explore the repository structure
   1.1 Understand the codebase structure and the `workload()` function.
   1.2 Identify the code paths that are being exercised by the workload.
   1.3 Look for performance bottlenecks and optimization opportunities.

Phase 2. BASELINE: create a script to measure performance
   2.1 Create a Python script to reproduce the performance workload.
   2.2 Execute it with `python <workload_file>` and examine the printed output metrics.
   2.3 Record the baseline performance for comparison.

Phase 3. ANALYSIS: identify optimization opportunities
   3.1 Profile the code if needed to identify bottlenecks.
   3.2 Look for inefficient algorithms, unnecessary computations, or optimization opportunities.
   3.3 Consider common optimization techniques: caching, vectorization, algorithmic improvements, etc.

Phase 4. OPTIMIZATION: implement changes
   4.1 Edit the source code of the repository to improve performance.
   4.2 Do NOT change the contents of the `workload()` function itself.
   4.3 Focus on optimizing the code in the repository that the `workload()` function uses.
   4.4 If non-Python changes were made, rebuild the repo to make sure the changes take effect.

Phase 5. VERIFICATION: test your optimizations
   5.1 Rerun your performance script to confirm that performance has improved.
   5.2 If necessary, identify any relevant test files in the repository related to your changes.
   5.3 Verify that test statuses did not change after your modifications.

Phase 6. ITERATION: refine if needed
   6.1 After each attempted change, reflect on the changes attempted and the performance impact observed.
   6.2 If the performance did not improve, consider alternative approaches or optimizations.
   6.3 Repeat until you are satisfied with the performance improvement.

Phase 7. FINALIZATION: complete the task
   7.1 Once you are satisfied with the performance improvement, use the finish command to complete your task.

Please remember that you should not change the implementation of the `workload()` function. The performance improvement should solely come from editing the source files in the code repository.
